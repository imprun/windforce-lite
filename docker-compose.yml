name: windforce-lite

x-windforce-lite-database-url: &windforce-lite-database-url "${WINDFORCE_LITE_DATABASE_URL:-postgres://postgres@postgres:5432/windforce_lite?sslmode=disable}"

services:
  postgres:
    profiles:
      - pg
      - standalone
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "127.0.0.1:${WINDFORCE_POSTGRES_PORT:-5432}:5432"
    environment:
      POSTGRES_DB: ${WINDFORCE_POSTGRES_DB:-windforce_lite}
      POSTGRES_USER: ${WINDFORCE_POSTGRES_USER:-postgres}
      POSTGRES_HOST_AUTH_METHOD: trust
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U \"$${POSTGRES_USER}\" -d \"$${POSTGRES_DB}\""]
      interval: 5s
      timeout: 3s
      retries: 20
    volumes:
      - postgres-data:/var/lib/postgresql/data

  control-plane:
    profiles:
      - backend
      - standalone
    image: ${WINDFORCE_LITE_IMAGE:-imprun/windforce-lite:local}
    build:
      context: .
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
        required: false
    ports:
      - "127.0.0.1:${WINDFORCE_LITE_API_PORT:-18090}:8080"
    command:
      - api
      - --addr
      - :8080
      - --state-backend
      - postgres
      - --database-url
      - *windforce-lite-database-url
      - --migrate
      - --store
      - /data/store
      - --catalog
      - /data/catalog.json
      - --git-sources
      - /data/git-sources.json
    volumes:
      - windforce-data:/data

  worker:
    profiles:
      - worker
      - standalone
    image: ${WINDFORCE_LITE_IMAGE:-imprun/windforce-lite:local}
    build:
      context: .
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
        required: false
    command:
      - worker
      - --state-backend
      - postgres
      - --database-url
      - *windforce-lite-database-url
      - --migrate
      - --store
      - /data/store
      - --cache
      - /data/cache
    volumes:
      - windforce-data:/data

volumes:
  postgres-data:
  windforce-data:
